# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Z.ai — main LLM + embeddings (OpenAI-compatible)
ZAI_API_KEY=
ZAI_BASE_URL=
ZAI_MODEL=

# Optional: override if Z.ai's coding endpoint doesn't serve /embeddings.
# (Default falls back to ZAI_BASE_URL.)
# ZAI_EMBEDDINGS_BASE_URL=
# ZAI_EMBEDDING_MODEL=embedding-3

# Mapbox
NEXT_PUBLIC_MAPBOX_TOKEN=

# Google Places
GOOGLE_PLACES_API_KEY=

# Brave Search (optional; research subagent degrades gracefully if unset)
BRAVE_SEARCH_API_KEY=
